[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-pkg-example"
version = "0.1.0"
description = "Example Python package"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
